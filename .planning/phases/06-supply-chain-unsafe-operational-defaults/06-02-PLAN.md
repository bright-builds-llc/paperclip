---
phase: 06-supply-chain-unsafe-operational-defaults
plan: "02"
type: execute
wave: 2
depends_on:
  - "06-01"
files_modified:
  - .planning/phases/06-supply-chain-unsafe-operational-defaults/06-PACKAGE-BUILD-PUBLISH-REVIEW.md
autonomous: true
requirements:
  - SUPP-01
user_setup: []
must_haves:
  truths:
    - Phase 6 documents the lockfile, workspace package, build, and publish path that turns the monorepo into public npm artifacts and release payloads.
    - The review distinguishes guarded release-train controls from places where hard-coded manifest rewriting, mutable prepack artifacts, or dependency-resolution policy can drift from runtime reality.
    - The artifact identifies whether released packages can ship incomplete dependencies, stale UI or skill artifacts, or other integrity problems that widen the trust boundary for users.
  artifacts:
    - .planning/phases/06-supply-chain-unsafe-operational-defaults/06-PACKAGE-BUILD-PUBLISH-REVIEW.md
  key_links:
    - The review links `scripts/generate-npm-package-json.mjs` to `cli/src/adapters/registry.ts` so CLI publish-manifest completeness is checked against the adapters the CLI actually imports.
    - The review links `scripts/build-npm.sh`, `scripts/prepare-server-ui-dist.sh`, and `scripts/release.sh` to package `files` and `publishConfig` fields so publish payload mutation is evaluated end to end.
---

<objective>
Create the package, build, and publish-chain review artifact for Phase 6.

Purpose: Determine whether Paperclip's lockfile policy, workspace manifests, build scripts, and release-time publish steps can ship incomplete or unsafe artifacts.
Output: `.planning/phases/06-supply-chain-unsafe-operational-defaults/06-PACKAGE-BUILD-PUBLISH-REVIEW.md`
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-threat-model-attack-surface-baseline/01-THREAT-MODEL.md
@.planning/phases/01-threat-model-attack-surface-baseline/01-AUDIT-METHODOLOGY.md
@.planning/phases/06-supply-chain-unsafe-operational-defaults/06-RESEARCH.md
@.planning/phases/06-supply-chain-unsafe-operational-defaults/06-OPERATIONAL-DEFAULTS-REVIEW.md
@package.json
@pnpm-workspace.yaml
@.npmrc
@.changeset/config.json
@.github/workflows/pr-policy.yml
@.github/workflows/refresh-lockfile.yml
@.github/workflows/pr-verify.yml
@.github/workflows/release.yml
@doc/PUBLISHING.md
@doc/RELEASING.md
@scripts/build-npm.sh
@scripts/generate-npm-package-json.mjs
@scripts/prepare-server-ui-dist.sh
@scripts/release.sh
@scripts/release-lib.sh
@cli/package.json
@cli/src/adapters/registry.ts
@server/package.json
@packages/plugins/sdk/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Review lockfile, package-manager, and workspace manifest policy</name>
  <files>.planning/phases/06-supply-chain-unsafe-operational-defaults/06-PACKAGE-BUILD-PUBLISH-REVIEW.md, package.json, pnpm-workspace.yaml, .npmrc, .changeset/config.json, .github/workflows/pr-policy.yml, .github/workflows/refresh-lockfile.yml, .github/workflows/pr-verify.yml, .github/workflows/release.yml, cli/package.json, server/package.json, packages/plugins/sdk/package.json</files>
  <action>Document the workspace package manager contract, bot-owned lockfile policy, frozen versus non-frozen install split, public versus private package inventory, fixed-versioning rules, package `files` and `publishConfig` boundaries, and any manifest hygiene issues already visible in the repo. Record which controls improve reproducibility and which leave room for dependency or package-shape drift.</action>
  <verify>Use `rg -n "packageManager|workspace:\\*|auto-install-peers|fixed|baseBranch|frozen-lockfile|no-frozen-lockfile|publishConfig|files|private|duplicate" package.json pnpm-workspace.yaml .npmrc .changeset/config.json .github/workflows/pr-policy.yml .github/workflows/refresh-lockfile.yml .github/workflows/pr-verify.yml .github/workflows/release.yml cli/package.json server/package.json packages/plugins/sdk/package.json`.</verify>
  <done>The review contains a lockfile-and-manifest section that makes the workspace package policy concrete.</done>
</task>

<task type="auto">
  <name>Task 2: Review build and publish artifact generation</name>
  <files>.planning/phases/06-supply-chain-unsafe-operational-defaults/06-PACKAGE-BUILD-PUBLISH-REVIEW.md, scripts/build-npm.sh, scripts/generate-npm-package-json.mjs, scripts/prepare-server-ui-dist.sh, scripts/release.sh, scripts/release-lib.sh, cli/src/adapters/registry.ts, cli/package.json, server/package.json, doc/PUBLISHING.md, doc/RELEASING.md</files>
  <action>Trace how the CLI bundle is built, how `cli/package.json` is rewritten for npm, how server UI assets and skills are copied into publish payloads, and how release cleanup restores dev state. Check whether the publish path stays complete and reproducible, especially where hard-coded workspace package lists, duplicate manifest keys, or temporary artifact copying could leak stale or maintainer-only content or omit runtime dependencies.</action>
  <verify>Use `rg -n "build-npm|generate-npm-package-json|package.dev.json|prepare-server-ui-dist|ui-dist|skills|restore_publish_artifacts|adapter-cursor-local|adapter-gemini-local|adapter-pi-local|publishable|trusted publishing" scripts/build-npm.sh scripts/generate-npm-package-json.mjs scripts/prepare-server-ui-dist.sh scripts/release.sh scripts/release-lib.sh cli/src/adapters/registry.ts cli/package.json server/package.json doc/PUBLISHING.md doc/RELEASING.md`.</verify>
  <done>The review contains a build-and-publish section that makes publish artifact mutation and completeness checks explicit.</done>
</task>

<task type="auto">
  <name>Task 3: Publish the package, build, and publish review artifact</name>
  <files>.planning/phases/06-supply-chain-unsafe-operational-defaults/06-PACKAGE-BUILD-PUBLISH-REVIEW.md</files>
  <action>Finalize `06-PACKAGE-BUILD-PUBLISH-REVIEW.md` as the second Phase 6 artifact. Include at minimum: scope, lockfile and package-manager model, public package inventory and manifest rules, build and publish artifact generation, findings or review leads, and later phase questions. Keep it explicit about what end users actually install and where release-time mutations could affect integrity.</action>
  <verify>Use `test -f .planning/phases/06-supply-chain-unsafe-operational-defaults/06-PACKAGE-BUILD-PUBLISH-REVIEW.md` and `rg -n "^## (Scope|Lockfile And Package-Manager Model|Public Package Inventory And Manifest Rules|Build And Publish Artifact Generation|Findings Or Review Leads|Later Phase Questions)$" .planning/phases/06-supply-chain-unsafe-operational-defaults/06-PACKAGE-BUILD-PUBLISH-REVIEW.md`.</verify>
  <done>`06-PACKAGE-BUILD-PUBLISH-REVIEW.md` exists with the required sections and enough detail to anchor supply-chain and publish-integrity findings.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `test -f .planning/phases/06-supply-chain-unsafe-operational-defaults/06-PACKAGE-BUILD-PUBLISH-REVIEW.md`
- [ ] `rg -n "^## (Scope|Lockfile And Package-Manager Model|Public Package Inventory And Manifest Rules|Build And Publish Artifact Generation|Findings Or Review Leads|Later Phase Questions)$" .planning/phases/06-supply-chain-unsafe-operational-defaults/06-PACKAGE-BUILD-PUBLISH-REVIEW.md`
- [ ] `rg -n "lockfile|workspace|publish|build-npm|ui-dist|skills|trusted publishing|adapter-cursor-local|adapter-gemini-local|adapter-pi-local" .planning/phases/06-supply-chain-unsafe-operational-defaults/06-PACKAGE-BUILD-PUBLISH-REVIEW.md`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The artifact makes dependency-resolution and publish-payload assumptions concrete across CI and maintainer release workflows
- The artifact leaves maintainers with a clear view of whether published packages can drift from the monorepo runtime surface
  </success_criteria>

<output>
After completion, create `.planning/phases/06-supply-chain-unsafe-operational-defaults/06-02-SUMMARY.md`
</output>
