---
phase: 06-supply-chain-unsafe-operational-defaults
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/06-supply-chain-unsafe-operational-defaults/06-OPERATIONAL-DEFAULTS-REVIEW.md
autonomous: true
requirements:
  - SUPP-01
user_setup: []
must_haves:
  truths:
    - Phase 6 documents the real onboarding, run, doctor, worktree, Docker, CI, and release-entrypoint defaults that operators and maintainers follow before a hardened deployment exists.
    - The review distinguishes explicit local-only or smoke-only sharp edges from defaults that can ship insecure auth, backup, or secret-handling posture into ordinary Paperclip setups.
    - The artifact ties earlier auth and data-surface findings to the operational workflows that can preserve, duplicate, or normalize those weak states.
  artifacts:
    - .planning/phases/06-supply-chain-unsafe-operational-defaults/06-OPERATIONAL-DEFAULTS-REVIEW.md
  key_links:
    - The review links `cli/src/commands/onboard.ts`, `cli/src/commands/run.ts`, and `cli/src/checks/deployment-auth-check.ts` so quickstart defaults, doctor gates, and runtime startup behavior are evaluated together.
    - The review links worktree seeding and Docker smoke flows to CI and release entrypoints in `.github/workflows/*.yml` and `doc/RELEASING.md` so operator defaults are not treated in isolation.
---

<objective>
Create the operational-defaults review artifact for Phase 6.

Purpose: Determine whether onboarding, run, doctor, worktree, Docker, CI, and release-entrypoint defaults can ship insecure states or erode trust boundaries before Paperclip is even running normally.
Output: `.planning/phases/06-supply-chain-unsafe-operational-defaults/06-OPERATIONAL-DEFAULTS-REVIEW.md`
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-threat-model-attack-surface-baseline/01-THREAT-MODEL.md
@.planning/phases/01-threat-model-attack-surface-baseline/01-AUDIT-METHODOLOGY.md
@.planning/phases/05-secrets-storage-data-exposure/05-VERIFICATION.md
@.planning/phases/06-supply-chain-unsafe-operational-defaults/06-RESEARCH.md
@doc/DEVELOPING.md
@doc/DOCKER.md
@doc/RELEASING.md
@cli/src/commands/onboard.ts
@cli/src/commands/run.ts
@cli/src/commands/doctor.ts
@cli/src/checks/deployment-auth-check.ts
@cli/src/commands/worktree.ts
@cli/src/commands/worktree-lib.ts
@server/src/startup-banner.ts
@scripts/docker-onboard-smoke.sh
@.github/workflows/pr-verify.yml
@.github/workflows/release.yml
@cli/src/__tests__/doctor.test.ts
@cli/src/__tests__/worktree.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Review onboarding, run, doctor, and deployment defaults</name>
  <files>.planning/phases/06-supply-chain-unsafe-operational-defaults/06-OPERATIONAL-DEFAULTS-REVIEW.md, cli/src/commands/onboard.ts, cli/src/commands/run.ts, cli/src/commands/doctor.ts, cli/src/checks/deployment-auth-check.ts, server/src/startup-banner.ts, doc/DEVELOPING.md, doc/DOCKER.md</files>
  <action>Trace quickstart defaults, `--yes` behavior, environment-aware overrides, automatic repair and startup flow, deployment/auth checks, bootstrap-invite timing, and startup messaging. Record which defaults enforce safe posture, which silently preserve convenience-first settings such as secret strict mode off or backups enabled, and which earlier findings those defaults could amplify.</action>
  <verify>Use `rg -n "quickstart|--yes|local_trusted|authenticated|strictMode|backup|bootstrap|repair|publicBaseUrl|allowedHostnames|PAPERCLIP_AGENT_JWT_SECRET|DB Backup" cli/src/commands/onboard.ts cli/src/commands/run.ts cli/src/commands/doctor.ts cli/src/checks/deployment-auth-check.ts server/src/startup-banner.ts doc/DEVELOPING.md doc/DOCKER.md`.</verify>
  <done>The review contains an onboarding-and-deployment section that makes setup defaults and their safety implications explicit.</done>
</task>

<task type="auto">
  <name>Task 2: Review worktree, Docker smoke, CI, and release-entrypoint assumptions</name>
  <files>.planning/phases/06-supply-chain-unsafe-operational-defaults/06-OPERATIONAL-DEFAULTS-REVIEW.md, cli/src/commands/worktree.ts, cli/src/commands/worktree-lib.ts, scripts/docker-onboard-smoke.sh, .github/workflows/pr-verify.yml, .github/workflows/release.yml, doc/RELEASING.md, cli/src/__tests__/worktree.test.ts</files>
  <action>Review worktree init or make defaults, seed-mode behavior, secrets-key copy, repo-local env loading, Docker smoke defaults, CI verification entrypoints, and release-train guardrails. Document which workflows are explicit trusted-operator or smoke-harness behavior and which defaults could normalize weak trust boundaries or duplicate sensitive state in everyday use.</action>
  <verify>Use `rg -n "seed|minimal|full|copySeededSecretsKey|PAPERCLIP_IN_WORKTREE|docker-onboard-smoke|SMOKE_AUTO_BOOTSTRAP|frozen-lockfile|release/X.Y.Z|trusted publishing|npm-release" cli/src/commands/worktree.ts cli/src/commands/worktree-lib.ts scripts/docker-onboard-smoke.sh .github/workflows/pr-verify.yml .github/workflows/release.yml doc/RELEASING.md cli/src/__tests__/worktree.test.ts`.</verify>
  <done>The review contains an operational-automation section that makes worktree, smoke, CI, and release-entrypoint trust assumptions concrete.</done>
</task>

<task type="auto">
  <name>Task 3: Publish the operational-defaults review artifact</name>
  <files>.planning/phases/06-supply-chain-unsafe-operational-defaults/06-OPERATIONAL-DEFAULTS-REVIEW.md</files>
  <action>Finalize `06-OPERATIONAL-DEFAULTS-REVIEW.md` as the first Phase 6 artifact. Include at minimum: scope, onboarding/run/doctor defaults, worktree/Docker/local-operator flows, CI and release entrypoint assumptions, findings or review leads, and later phase questions. Keep it explicit about which defaults are intentional sharp edges versus defaults that can ship insecure deployments or operational copies of sensitive state.</action>
  <verify>Use `test -f .planning/phases/06-supply-chain-unsafe-operational-defaults/06-OPERATIONAL-DEFAULTS-REVIEW.md` and `rg -n "^## (Scope|Onboarding, Run, And Doctor Defaults|Worktree, Docker, And Local-Operator Flows|CI And Release Entrypoint Assumptions|Findings Or Review Leads|Later Phase Questions)$" .planning/phases/06-supply-chain-unsafe-operational-defaults/06-OPERATIONAL-DEFAULTS-REVIEW.md`.</verify>
  <done>`06-OPERATIONAL-DEFAULTS-REVIEW.md` exists with the required sections and enough detail to anchor operator-default findings.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `test -f .planning/phases/06-supply-chain-unsafe-operational-defaults/06-OPERATIONAL-DEFAULTS-REVIEW.md`
- [ ] `rg -n "^## (Scope|Onboarding, Run, And Doctor Defaults|Worktree, Docker, And Local-Operator Flows|CI And Release Entrypoint Assumptions|Findings Or Review Leads|Later Phase Questions)$" .planning/phases/06-supply-chain-unsafe-operational-defaults/06-OPERATIONAL-DEFAULTS-REVIEW.md`
- [ ] `rg -n "local_trusted|strict mode|backup|worktree|docker|release|doctor|run" .planning/phases/06-supply-chain-unsafe-operational-defaults/06-OPERATIONAL-DEFAULTS-REVIEW.md`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The artifact makes setup and operator defaults concrete across CLI, Docker, CI, and release-entrypoint workflows
- The artifact leaves maintainers with a clear view of which operational flows should remain accepted sharp edges and which should be treated as unsafe shipped defaults
  </success_criteria>

<output>
After completion, create `.planning/phases/06-supply-chain-unsafe-operational-defaults/06-01-SUMMARY.md`
</output>
