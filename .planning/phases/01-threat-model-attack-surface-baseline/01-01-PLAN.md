---
phase: 01-threat-model-attack-surface-baseline
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/01-threat-model-attack-surface-baseline/01-THREAT-MODEL.md
autonomous: true
requirements:
  - THRT-01
user_setup: []
must_haves:
  truths:
    - Phase 1 has a repo-grounded threat model that names principals, deployment modes, trust assumptions, and protected assets.
    - The document separates intentional local-only sharp edges from authenticated or remotely reachable risk surfaces.
    - Every trust boundary in scope is tied to concrete enforcement or exposure files.
  artifacts:
    - .planning/phases/01-threat-model-attack-surface-baseline/01-THREAT-MODEL.md
  key_links:
    - The threat model links board, agent, plugin, local-operator, and release actors to the files that grant, enforce, or expand their authority.
    - The threat model links deployment-mode semantics from docs to the current code paths that implement them.
---

<objective>
Create the threat-model baseline for the security audit.

Purpose: Fix the vocabulary for principals, deployment modes, assets, and trust boundaries before deeper exploit review begins.
Output: `.planning/phases/01-threat-model-attack-surface-baseline/01-THREAT-MODEL.md`
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-threat-model-attack-surface-baseline/01-RESEARCH.md
@doc/PRODUCT.md
@doc/SPEC-implementation.md
@doc/DEPLOYMENT-MODES.md
@server/src/config.ts
@server/src/middleware/auth.ts
@server/src/routes/authz.ts
@server/src/auth/better-auth.ts
@server/src/realtime/live-events-ws.ts
@server/src/routes/access.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enumerate principals, assets, and deployment assumptions</name>
  <files>.planning/phases/01-threat-model-attack-surface-baseline/01-THREAT-MODEL.md, doc/DEPLOYMENT-MODES.md, server/src/config.ts, server/src/middleware/auth.ts, server/src/auth/better-auth.ts</files>
  <action>Derive the authoritative principal list for the audit from repo evidence: local-trusted board, authenticated board user, instance admin, company member, agent principal, plugin author/worker, local CLI operator, and release actor. Capture the protected assets each can reach and the deployment assumptions that change security posture, including `local_trusted`, `authenticated/private`, and `authenticated/public`. Be explicit about current defaults such as implicit board auth, Better Auth secret fallback, signup controls, hostname allowlists, backup defaults, and company-deletion behavior.</action>
  <verify>Use `rg -n "local_trusted|authenticated|companyDeletionEnabled|BETTER_AUTH_SECRET|disableSignUp|allowedHostnames" doc/DEPLOYMENT-MODES.md server/src/config.ts server/src/middleware/auth.ts server/src/auth/better-auth.ts` to confirm every major mode/default claim is backed by source.</verify>
  <done>The draft threat model has a principal table and a deployment-mode section grounded in current docs and code, not generic security language.</done>
</task>

<task type="auto">
  <name>Task 2: Map trust boundaries and enforcement points</name>
  <files>.planning/phases/01-threat-model-attack-surface-baseline/01-THREAT-MODEL.md, server/src/middleware/auth.ts, server/src/routes/authz.ts, server/src/realtime/live-events-ws.ts, server/src/routes/access.ts</files>
  <action>Document the boundary transitions that later phases will test: request to actor resolution, actor to company authorization, session to websocket upgrade, board actions to company membership and invite flows, and server to plugin or agent runtime boundaries. For each boundary, name the responsible files, what guarantee they appear to provide, and what a later exploit-focused phase must verify.</action>
  <verify>Use `rg -n "actorMiddleware|assertCompanyAccess|assertBoard|authorizeUpgrade|claimBoardOwnership|companyMemberships" server/src/middleware/auth.ts server/src/routes/authz.ts server/src/realtime/live-events-ws.ts server/src/routes/access.ts` to confirm the documented boundaries map to real enforcement points.</verify>
  <done>The document contains a trust-boundary section with concrete file references and a clear statement of the expected guarantee at each boundary.</done>
</task>

<task type="auto">
  <name>Task 3: Publish the Phase 1 threat model artifact</name>
  <files>.planning/phases/01-threat-model-attack-surface-baseline/01-THREAT-MODEL.md</files>
  <action>Finalize `01-THREAT-MODEL.md` as a maintainer-usable artifact. Include: scope, protected assets, principals, deployment modes, trust boundaries, assumptions, out-of-scope notes, and handoff notes for later phases. Keep the writing compact and file-grounded so later audit plans can cite this document without re-deriving the model.</action>
  <verify>Use `test -f .planning/phases/01-threat-model-attack-surface-baseline/01-THREAT-MODEL.md` and `rg -n "^## (Scope|Protected Assets|Principals|Deployment Modes|Trust Boundaries|Assumptions|Out of Scope|Later Phase Questions)$" .planning/phases/01-threat-model-attack-surface-baseline/01-THREAT-MODEL.md`.</verify>
  <done>`01-THREAT-MODEL.md` exists with the required sections and enough repo-specific detail that later phases can use it as the canonical baseline.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `test -f .planning/phases/01-threat-model-attack-surface-baseline/01-THREAT-MODEL.md`
- [ ] `rg -n "^## (Scope|Protected Assets|Principals|Deployment Modes|Trust Boundaries|Assumptions|Out of Scope|Later Phase Questions)$" .planning/phases/01-threat-model-attack-surface-baseline/01-THREAT-MODEL.md`
- [ ] `rg -n "local_trusted|authenticated|agent|plugin|company|websocket|worktree" .planning/phases/01-threat-model-attack-surface-baseline/01-THREAT-MODEL.md`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The threat model distinguishes principal types, deployment modes, and trust boundaries with concrete evidence references
- The artifact is usable as the baseline document for Phases 2 through 6
  </success_criteria>

<output>
After completion, create `.planning/phases/01-threat-model-attack-surface-baseline/01-01-SUMMARY.md`
</output>
